# react-json-view@1.21.3 declares an outdated peer range (react <=17) but works
# fine under the project's react@18. Without this, npm 7+ aborts install/ci with
# ERESOLVE. legacy-peer-deps uses the npm 6 resolution so install just works.
legacy-peer-deps=true
